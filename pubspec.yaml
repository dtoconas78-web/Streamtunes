name: streamtunes
description: "App de streaming musical gratuito y sin límites para artista independientes, con estadísticas de reproducciones para cada artistas."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 0.1.0+1

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  
  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true
